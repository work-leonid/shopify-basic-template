---
export interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<link
			rel="stylesheet"
			href="https://cdn.jsdelivr.net/npm/swiper@8/swiper-bundle.min.css"
		/>

		<title>{title}</title>
	</head>
	<body class="text-xl relative text-slate-800">
		<!-- <div -->
		<!-- 	class="hidden absolute left-1/2 -z-10 h-[35em] bg-gradient-radial blur-3xl from-blue-500/40 rounded-full aspect-square" -->
		<!-- > -->
		<!-- </div> -->
		<!-- <div -->
		<!-- 	class="absolute left-1/3 top-40 -z-10 h-96 bg-gradient-radial blur-3xl from-cyan-500/20 rounded-full aspect-square" -->
		<!-- > -->
		<!-- </div> -->
		<slot />
	</body>
</html>
